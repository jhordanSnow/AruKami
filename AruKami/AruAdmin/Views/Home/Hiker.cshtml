
@model IEnumerable<AruAdmin.Models.View_Hiker>

@{
    ViewBag.Title = "Hikers";
}

<div class="box">
    <div class="box-header">
        <h3 class="box-title">Hikers</h3>
    </div>
    <div class="box-body">
        <table id="checkout" class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>@Html.DisplayName("Name")</th>
                    <th>@Html.DisplayName("Username")</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>@item.BirthDate</td>
                        <td>@item.Gender</td>
                        <td>@item.Nationality</td>
                        <td align="center">
                            @{
                                if (@item.Inactive == 0)
                                {
                                    <a class="btn btn-danger fa fa-ban" data-toggle="modal" data-target="#modalDelete" data-name="@item.Name" data-type-id="5" data-item-id="@item.IdCard"></a>
                                }
                                else
                                {
                                    <a class="btn btn-success fa fa-check-circle" data-toggle="modal" data-target="#modalActivar" data-name="@item.Name" data-type-id="5" data-item-id="@item.IdCard"></a>
                                }
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>



<div class="modal modal-danger" id="modalDelete">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
                <h4 class="modal-title"></h4>
            </div>
            <form id="deleteForm" action="" method="post">
                <div class="modal-body">
                    <p>Do you really wanna delete the item?</p>
                    <p><i>Note: if the item is been used is gonna be disabled.</i></p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline pull-left" data-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-outline">Delete</button>
                </div>
            </form>
        </div>
        <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
</div>






<div class="modal modal-success" id="modalActivar">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
                <h4 class="modal-title"></h4>
            </div>
            <form id="activarForm" action="" method="post">
                <div class="modal-body">
                    <p>activar esta mierda ?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline pull-left" data-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-outline">Activate</button>
                </div>
            </form>
        </div>
        <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
</div>


@section Scripts {
    @Scripts.Render("~/AdminLTE/plugins/datatables/js")
    @Scripts.Render("~/Scripts/Checks/Table")
    @Scripts.Render("~/Scripts/Catalog/Index")
}
